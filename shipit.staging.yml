# kubernetes:
#   namespace: rubygems-staging
#   context: rubygems

deploy:
  override:
    - kubernetes-deploy rubygems-staging rubygems --template-dir config/deploy/staging
    - cap $ENVIRONMENT deploy
